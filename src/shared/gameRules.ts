// Public shared-rules entrypoint for browser code and server-side tooling.
// The implementation remains platform-neutral and is also imported by the
// Supabase edge functions through the same module boundary.
export * from '../../supabase/functions/_shared/gameRules';
